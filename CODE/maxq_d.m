function [y, g] = maxq_d(x)
%-----------------------------------------
% Function Maxq
%	n = 20		m = 20
%	f(x*) = 0.0
%-----------------------------------------
	for i = 1:20
		f(i) = x(i)^2;
    end
    y = max(f);
    J = 1:(length(x)-1);
    g = x(J).^2 + x(J+1).^2 + x(J).*x(J+1) - 1;
end
